# 项目总指挥

本文件是仓库固定执行入口。每次执行任何操作前，先读本文件；是否继续读取子目录入口，由当前任务、所在目录和改动风险决定。

## 三大来源

项目长期可信来源只有三类：

- 主实现规格：业务语义源，记录目标、流程、边界、规则、验收和关键不变量。
- 代码：实现事实源，记录当前真实机制、可观察行为、测试和必要技术说明。
- Git 历史：变更追溯源，记录演化轨迹、变更原因和变更范围。

AI 对话、临时计划和调试过程不作为长期依据；稳定决策进入主实现规格或代码，真实变动必须能从 Git 历史追溯。

## 第一性原则

- 先确认真实文件、代码、运行状态和实现依据，再做结论。
- 一切判断回到最终目的：真实用户、真实问题、可感知结果和验收方式。
- 新增功能、组件、状态、依赖、文档或流程时，必须说清它服务的真实需求；说不清，优先不新增。
- 保持最小闭环；本质复杂度不可消灭，偶然复杂度应持续压低。
- 如果开发者方向偏离最终目的、扩大范围或增加无关复杂度，先简短提示风险，再给出更小的可验证方案。

## 规格与代码边界

主实现规格只记录业务语义，不复述当前代码做法。

实现规格的写入、回写和拆分规则见 `docs/spec/AGENTS.md`。

凡涉及代码、测试、业务行为、用户流程、接口、数据语义、状态变化、失败处理或验收标准的任务，必须读取 `docs/spec/AGENTS.md`，并在最终回复中按其规则输出主实现规格回写状态。

## 代码维护约束

- 页面文件原则上不超过 600 行；UI 组件文件原则上不超过 400 行。超过前优先按职责拆分，除非拆分会明显降低可读性。
- 可复用 UI、状态逻辑、协议模型和业务规则必须抽离复用，避免在页面中堆叠重复代码。
- 页面负责装配和路由，复杂状态变化、工具编排、权限判断和数据转换应下沉到 controller、service、domain 或 adapter。
- 当复杂度可以通过成熟设计模式显著降低时，优先使用清晰的模式，例如 controller/view、registry、adapter、policy、store，而不是临时分支堆叠。
- `flutter analyze` 必须保持通过；分析规则应偏严格，新增忽略规则需要有明确理由。

## 目录入口

- `docs/AGENTS.md`：文档区入口，规定长期文档分类和治理规则。
- `docs/spec/AGENTS.md`：实现规格目录入口，规定主实现规格的写入边界、回写规则和拆分规则。
- `docs/appendix/README.md`：附录说明，面向理解，不作为实现依据。

## 执行流程

1. 先读本文件。
2. 判断项目状态：
   - 空项目或缺少主实现规格时，先与开发者澄清目标、边界、核心流程和验收标准，形成或补齐主实现规格；未确认前不进入代码实现。
   - 已有实现时，先读代码现状和相关规格；若发现实现需要改变规格，或规格与代码现实冲突，按 `docs/spec/AGENTS.md` 处理。
3. 非平凡改动前做第一性原理思考：确认用户结果、最小必要范围、验收方式和明确不做的内容。
4. 实现时优先保持最小闭环，避免引入与当前目标无关的抽象、状态、依赖、文档和流程。
5. 完成前运行与改动风险匹配的验证；验证失败时先修复并复验。
6. 完成前检查是否需要回写主实现规格；只要本次任务触及上方列出的行为或语义范围，最终回复必须包含 `docs/spec/AGENTS.md` 定义的三种回写状态之一。
7. 继续开发时，以主实现规格、代码现状、测试结果和 Git 历史为依据，不依赖临时执行记录。
